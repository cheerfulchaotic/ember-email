import { primaryListing } from 'email/helpers/primary-listing';
import { module, test } from 'qunit';

module('Unit | Helper | primary listing');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = primaryListing([42]);
  assert.ok(result);
});
